<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}} - Documentation</title>
    <link rel="stylesheet" href="/styles/main.css">
</head>
<body class="bg-gray-50 dark:bg-gray-900">
    {{> header}}

    <div class="docs-container flex">
        <!-- Sidebar -->
        <aside class="sidebar w-64 bg-white dark:bg-gray-800 min-h-screen border-r border-gray-200 dark:border-gray-700 fixed left-0 top-16 overflow-y-auto">
            {{> sidebar}}
        </aside>

        <!-- Main content -->
        <main class="main-content flex-1 ml-64 mr-64 p-8">
            <article class="max-w-4xl mx-auto prose prose-lg dark:prose-invert">
                {{{content}}}
            </article>

            <!-- Edit this page link -->
            <div class="mt-12 pt-8 border-t border-gray-200 dark:border-gray-700 max-w-4xl mx-auto">
                <div class="flex items-center justify-between text-sm">
                    <a href="#" class="text-gray-600 dark:text-gray-400 hover:text-blue-600 dark:hover:text-blue-400 flex items-center gap-2">
                        <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
                            <path d="M13.586 3.586a2 2 0 112.828 2.828l-.793.793-2.828-2.828.793-.793zM11.379 5.793L3 14.172V17h2.828l8.38-8.379-2.83-2.828z"></path>
                        </svg>
                        Edit this page
                    </a>
                    <div class="text-gray-500 dark:text-gray-400">Last updated: {{date}}</div>
                </div>
            </div>
        </main>

        <!-- Table of Contents -->
        <aside class="toc-sidebar w-64 fixed right-0 top-16 h-screen overflow-y-auto border-l border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-800 hidden xl:block">
            <nav class="p-6">
                <h3 class="text-xs font-semibold text-gray-900 dark:text-gray-100 uppercase tracking-wide mb-3">On this page</h3>
                <div id="toc-content" class="toc-links space-y-2 text-sm">
                    <!-- TOC will be generated by JavaScript -->
                </div>
            </nav>
        </aside>
    </div>

    <script src="/script.js"></script>
</body>
</html>
